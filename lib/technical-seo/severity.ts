/**
 * Phase 27 — this module's actual implementation moved to
 * lib/category-engine/severity.ts (a second category engine,
 * lib/architecture/, needed the exact same severity-adjustment rules, and
 * none of them were ever Technical-SEO-specific). Re-exported here
 * unchanged so no existing Technical SEO import needs to change.
 */
export * from '@/lib/category-engine/severity'
